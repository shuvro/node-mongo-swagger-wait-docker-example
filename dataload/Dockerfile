FROM mongo:latest
ARG MONGO_HOST
ARG MONGO_DATABASE_USERNAME
ARG MONGO_DATABASE_PASSWORD
ARG MONGO_DB

COPY kyc /dataload/kyc
CMD mongorestore --host ${MONGO_HOST}  -u ${MONGO_DATABASE_USERNAME} -p ${MONGO_DATABASE_PASSWORD} -d ${MONGO_DB} /dataload/kyc